services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: escribe
      POSTGRES_PASSWORD: escribe
      POSTGRES_DB: escribe
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
